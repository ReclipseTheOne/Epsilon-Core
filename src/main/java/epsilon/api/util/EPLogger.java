package epsilon.api.util;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
public class EPLogger {
    public static Logger logger = LogManager.getLogger("Epsilon");

    private EPLogger() {}
}
